# Roadmap Well artwork

Place the five aligned Roadmap dark/lit desktop and mobile AVIF sets in this
directory. The exact filenames and source-master guidance are documented in
the parent `README.md` and configured in `mimir/scripts/roadmap-chapters.js`.

Placeholder gradients remain active until a complete phase set is present and
its `assetReady` flag is changed to `true`.
