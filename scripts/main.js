/* ==========================================================================
   Main JS — Global navigation, utilities, and shared behaviour
   ========================================================================== */

/**
 * Initialise global functionality once the DOM is ready.
 * All JS is progressive enhancement — the site works without it.
 */
document.addEventListener('DOMContentLoaded', function () {
    // Future: navigation toggle, scroll behaviour, shared utilities
});
